(function (angular) {
    'use strict';

    angular
        .module('my-app.home')
        .controller('HomeCtrl', HomeCtrl);

    HomeCtrl.$inject = ['$scope'];
    function HomeCtrl(   $scope) {

    }

}(window.angular));
